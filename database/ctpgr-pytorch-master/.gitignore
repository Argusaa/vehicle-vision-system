.idea
__pycache__
generated

# Checkpoints are training artifacts by default. The production YOLO11s LSTM
# and its manifest are the only checkpoints distributed with the application.
checkpoints/*
!checkpoints/lstm_yolo11s.pt
!checkpoints/model_manifest.json
